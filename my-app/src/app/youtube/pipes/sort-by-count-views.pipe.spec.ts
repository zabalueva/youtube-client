import { SortByCountViewsPipe } from './sort-by-count-views.pipe';

describe('SortByCountViewsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByCountViewsPipe();
    expect(pipe).toBeTruthy();
  });
});
